# Normalize line endings to LF in the repo and working tree so Prettier's
# default (endOfLine: "lf") stays consistent across platforms.
* text=auto eol=lf

# Binary/data files: let Git detect, don't touch line endings.
*.srt -text
